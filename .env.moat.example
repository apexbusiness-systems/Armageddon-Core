# ARMAGEDDON MOAT SECRETS (HYBRID CLOUD)
# CLASSIFICATION: TOP SECRET (DO NOT COMMIT)

# Supabase Keys (REQUIRED)
SUPABASE_URL=https://qhjqselqpkfqjfpuxykb.supabase.co
SUPABASE_SERVICE_ROLE_KEY=insert_your_service_role_key_here

# Temporal Cloud Configuration
TEMPORAL_ADDRESS=us-central1.gcp.api.temporal.io:7233
TEMPORAL_NAMESPACE=quickstart-armageddon-test.i7ero
TEMPORAL_TASK_QUEUE=armageddon-level-7

# Auth: API Key (Preferred if mTLS is locked) OR Certs
TEMPORAL_API_KEY=your_temporal_cloud_api_key

# Simulation Mode
SIM_MODE=true
SANDBOX_TENANT=armageddon-test-cloud

# Database (Local Persistence for Worker results, optional if using Cloud DB)
POSTGRES_USER=temporal
POSTGRES_PASSWORD=temporal
