# ARMAGEDDON Kinetic Engine Dependencies
# DATE: 2026-02-06
# PURPOSE: Python packages for real adversarial testing

# Core adversarial testing framework
garak==0.9.0

# Async HTTP for stress testing
aiohttp==3.9.1

# Numerical operations
# Numerical operations
# Pinned within the 1.26.x line this codebase targets, not latest (numpy 2.x
# has breaking API changes untested against garak==0.9.0 / this Python engine).
numpy==1.26.4

# Additional utilities
tenacity==8.2.3  # Retry logic — latest patch within the already-declared 8.2.x floor
structlog==24.1.0  # Structured logging — exact pin at the already-declared floor
