node_modules/
.next/
.env
.env.local
.DS_Store
dist/
build/
coverage/
.turbo/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

.env.moat
.env
.env.*
!.env.example
!.env.*.example

# Local attestation demo summary (generated on-demand)
attestation-demo-evidence.json

# TypeScript incremental build cache (generated)
*.tsbuildinfo

# Test run outputs
results/

# Plaintext credential dumps that don't match the .env* pattern above.
# ATSC-env.md (2026-07-16) holds live Cloudflare/Supabase/Temporal/GitHub
# tokens and a site admin password in Markdown — the .env* gitignore rules
# above do not catch this extension. Never commit this file; move its
# contents to a password manager or Render/Cloudflare secret store instead.
ATSC-env.md
*-env.md
*.secrets.md
